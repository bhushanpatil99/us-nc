version: 0.2

phases:
  install:
    commands:
      - echo "Install phase - nothing to install"
  build:
    commands:
      - echo "Build started on `date`"
      - mkdir -p build
      - cp index.html build/

artifacts:
  files:
    - build/index.html
